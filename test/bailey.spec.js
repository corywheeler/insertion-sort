
describe('Bailey', () => {
    it('should run', () => {
        console.log('i run');
        expect({a: 1}).to.not.have.property('b');
        foo = 1
            , beverages = { tea: [ 'chai', 'matcha', 'oolong' ] };

        foo.should.be.a('number');
        foo.should.equal(1);
    })
})